select the address bar at the top of the file in the file folder browser
type in cmd, or open a command line and navigate to the directory of the file

type java -jar jarjar.jar 
-f or -F followed by file_name (the file name and path) with no spaces
-t or -T followed by v (volume), h (height) or a (base area) with no spaces
-s or -S followed by b (bubble), s (selection), i (insertion), m (merge), q (quick) or h
(heap) with no spaces


